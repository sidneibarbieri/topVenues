# Demonstration shot plan

| Time | Screen and action | Proof shown |
| --- | --- | --- |
| 0:00–0:27 | The list a review could include, and how it moves | Same declared scope, one month apart: 427 more papers |
| 0:27–0:51 | The idea: declare, build once, freeze, ship the query | What the tool actually does, in four steps |
| 0:51–1:14 | One program, one file, and where the abstracts come from | DBLP for identity; open APIs first, publisher pages for the rest |
| 1:14–1:34 | A fresh clone and one command | The fingerprint checked, 407 tests, the search run |
| 1:34–2:14 | Overview, ranked search, the scope above the results | What was searched, not only what was found, plus BibTeX |
| 2:14–2:24 | Abstract coverage by venue | The gap declared source by source |
| 2:24–2:32 | What a fixed list makes measurable, and the links | 29.2% early signal, the 16.5x rule, and where to get it |

Production source is `narration.json`: one entry per narration segment, with
its measured audio duration. Shot lengths follow those durations, so the
picture is cut to the voice rather than to a fixed grid. The build that
produces all of it lives in the research tree, under `video/build-v4/`.
